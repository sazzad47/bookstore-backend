tags :
  - Book
summary: Save a new book
description: Save a new book
produces:
  - application/json
consumes:
  - application/json
parameters:
  - 
    in: body
    name: body
    description: Book object
    required: true
    schema:
      type: object
      $ref: ../../bodies/Book.yaml
  
responses:
  200:
    description: Book saved successfully
    schema:
      type: object
      $ref: ../../definitions/CommonResponse.yaml
  400:
    description: Error in saving book
    schema:
      type: object
      $ref: ../../definitions/CommonResponse.yaml